{
  "name": "construction-management-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "backend": "npm --prefix backend run dev",
    "backend:start": "npm --prefix backend run start",
    "frontend": "npm --prefix frontend run dev",
    "frontend:build": "npm --prefix frontend run build"
  }
}
